import "reflect-metadata";
// import { FFMpeg } from "./tools/ffmpeg";
// import { DiContainer } from "./util/di";

// const registeredTypes = {
//   ffmpeg: FFMpeg,
// };

// export const services = new DiContainer();

// export function register() {
//   Object.entries(registeredTypes).forEach(([key, value]) => {
//     services.register(key, value);
//   });
// }

// export function resolve<T extends keyof typeof registeredTypes>(
//   type: T,
//   args: ConstructorParameters<typeof registeredTypes[T]>
// ) {
//   return services.resolve(type, args);
// }
