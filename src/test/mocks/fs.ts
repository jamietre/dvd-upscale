// import { MakeDirectoryOptions, PathLike } from "node:fs";
// import { Fs } from "../../util/node/fs";

// function mkdir(
//   path: PathLike,
//   options: MakeDirectoryOptions & { recursive: true }
// ): Promise<string | undefined>;

// export class MockFs implements Required<Fs> {
//   rootfs: { [key: string]: any } = {};
//   cwd = "/";
//   mkdir(
//     path: PathLike,
//     options: MakeDirectoryOptions & { recursive: true }
//   ): Promise<string | undefined> {

//   }
// }

// todo: get utility for mapping path/to/object/props
